[package]
name = "pyadagram"
version = "0.1.0"
edition = "2024"

[dependencies]
pyo3 = { version = "0.24.0", features = ["extension-module", "abi3"] }
adagram = { git = "https://github.com/ondra/adagram" }

[lib]
crate-type = ["cdylib"]

